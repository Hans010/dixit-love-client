import {makeStyles} from "@material-ui/core";

export default makeStyles(theme => ({
    storyFormWrapper: {
        width: '20vw',
        background: "dimgray"
    },
    storyText: {
        color: "green",
    },
    storyWrapper: {
        backgroundColor: "red"
    }
}));