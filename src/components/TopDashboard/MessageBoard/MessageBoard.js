import React from 'react';
import useStyles from './styles';

const MessageBoard = (props) => {

    const classes = useStyles();

    return (
        <p>Add your magic here</p>
    );
}
export default MessageBoard;