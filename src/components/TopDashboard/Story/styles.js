import {makeStyles} from "@material-ui/core";

export default makeStyles(theme => ({
    storyFormWrapper: {
        width: '40vw',
        background: "dimgray",
        margin: '0 2rem'
    },
    storyText: {
        color: "green",
    },
    storyWrapper: {
        backgroundColor: "red"
    }
}));