import {makeStyles} from "@material-ui/core/styles";

export default makeStyles (theme => ({
     table: {
         minHeight: '400px',
         minWidth: '90%',
         display: "flex",
         flexFlow: 'row nowrap',
         justifyContent: "center",
         alignItems: "center",
         paddingTop: '3rem'
     }

    }));